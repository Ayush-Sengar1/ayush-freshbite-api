const router=require('express').Router();const auth=require('../controllers/authController');const pub=require('../controllers/publicController');const cart=require('../controllers/cartController');const orders=require('../controllers/orderController');const profile=require('../controllers/profileController');const admin=require('../controllers/adminController');const {requireAuth,requireAdmin,optionalAuth}=require('../middleware/auth');router.get('/health',(req,res)=>res.json({ok:true,service:'freshbite-api'}));router.post('/auth/register',auth.register);router.post('/auth/login',auth.login);router.post('/auth/logout',auth.logout);router.get('/auth/me',optionalAuth,auth.me);router.get('/categories',pub.categories);router.get('/dishes',pub.dishes);router.get('/dishes/:id',pub.dish);router.get('/dishes/:dishId/reviews',pub.reviews);router.get('/settings',pub.settings);router.get('/cart',requireAuth,cart.get);router.post('/cart/items',requireAuth,cart.add);router.put('/cart/items/:dishId',requireAuth,cart.update);router.delete('/cart/items/:dishId',requireAuth,cart.remove);router.post('/orders',requireAuth,orders.create);router.get('/orders',requireAuth,orders.list);router.get('/orders/:id',requireAuth,orders.get);router.post('/orders/:id/reorder',requireAuth,orders.reorder);router.get('/addresses',requireAuth,profile.addresses);router.post('/addresses',requireAuth,profile.addAddress);router.get('/favorites',requireAuth,profile.favorites);router.post('/favorites/:dishId',requireAuth,profile.addFavorite);router.delete('/favorites/:dishId',requireAuth,profile.removeFavorite);router.post('/reviews',requireAuth,profile.createReview);router.post('/coupons/validate',requireAuth,(req,res)=>{try{const service=require('../services/orderService');const result=service.validateCoupon(req.body.code,Number(req.body.subtotal||0),req.user._id);res.json({discount:result.discount,coupon:result.coupon?.code||null})}catch(e){res.status(422).json({error:e.message})}});router.use('/admin',requireAdmin);router.get('/admin/stats',admin.stats);router.get('/admin/dishes',admin.dishes);router.post('/admin/dishes',admin.createDish);router.put('/admin/dishes/:id',admin.updateDish);router.get('/admin/orders',admin.orders);router.patch('/admin/orders/:id/status',orders.updateStatus);router.get('/admin/customers',admin.customers);router.get('/admin/coupons',admin.coupons);router.get('/admin/settings',admin.settings);router.put('/admin/settings',admin.settings);module.exports=router;
